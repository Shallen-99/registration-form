What is the difference between using HTML5 validation attributes and JavaScript-based validation? Why might you use both?
HTML validations seem to be linear as in they have one purpose and serve that one purpose while JS validations seem to be customizable but you still need both since there are some things are easier to do with HTML and somethings have to be done by JS

Explain how you used localStorage to persist and retrieve the username. What are the limitations of localStorage for storing sensitive data?
I used localStorage.setItem() and localStorage.getItem to persist and retrieve the username, and the limitations on localStorage is that it's only for the specific browser you're using so for instance if you switch from chrome to safari it would not be there.

Describe a challenge you faced in implementing the real-time validation and how you solved it.
I had to do research on validation since this was my first time implementing it into code so the hardest part was reading the documentation and figuring out how to integrate it into my code correctly

How did you ensure that custom error messages were user-friendly and displayed at the appropriate times?
I used addEventListener("input") that was when they started to input the info it would let them know when if they met the requirements
